<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:MovingWordpress.Views"
        xmlns:ViewModels="clr-namespace:MovingWordpress.ViewModels" x:Class="MovingWordpress.Views.BeforeWordpressV"
        mc:Ignorable="d"
        Title="BeforeWordpressV" Height="800" Width="1200">
    <Window.DataContext>
        <ViewModels:BeforeWordpressVM/>
    </Window.DataContext>
    <b:Interaction.Triggers>
        <b:EventTrigger>
            <b:CallMethodAction MethodName="Init" TargetObject="{Binding}"/>
        </b:EventTrigger>
    </b:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Margin="5" BorderBrush="AliceBlue" BorderThickness="5" CornerRadius="5">
            <TextBlock Margin="5" VerticalAlignment="Center" HorizontalAlignment="Center" Text="荷づくり画面" FontSize="30"/>
        </Border>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <GroupBox Header="設定値">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Margin="5" Width="100" VerticalAlignment="Center" Text="ホスト名"/>
                                <TextBlock Margin="5" VerticalAlignment="Center" Text=":"/>
                                <TextBox Margin="5" Width="100" VerticalAlignment="Center" Text="{Binding SSHConnection.HostName}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Margin="5" Width="100" VerticalAlignment="Center" Text="ポート"/>
                                <TextBlock Margin="5" VerticalAlignment="Center" Text=":"/>
                                <TextBox Margin="5" Width="100" VerticalAlignment="Center" Text="{Binding SSHConnection.Port}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Margin="5" Width="100" VerticalAlignment="Center" Text="ユーザー名(サーバー)"/>
                                <TextBlock Margin="5" VerticalAlignment="Center" Text=":"/>
                                <TextBox Margin="5" Width="100" VerticalAlignment="Center" Text="{Binding SSHConnection.UserName}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Margin="5" Width="100" VerticalAlignment="Center" Text="パスワード(サーバー)"/>
                                <TextBlock Margin="5" VerticalAlignment="Center" Text=":"/>
                                <TextBox Margin="5" Width="100" VerticalAlignment="Center" Text="{Binding SSHConnection.PassWord}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Margin="5" Width="100" VerticalAlignment="Center" Text="パスフレーズ"/>
                                <TextBlock Margin="5" VerticalAlignment="Center" Text=":"/>
                                <TextBox Margin="5" Width="100" VerticalAlignment="Center" Text="{Binding SSHConnection.PassPhrase}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Margin="5" Width="100" VerticalAlignment="Center" Text="秘密鍵のパス(.pem)"/>
                                <TextBlock Margin="5" VerticalAlignment="Center" Text=":"/>
                                <TextBox Margin="5" Width="350" VerticalAlignment="Center" Text="{Binding SSHConnection.KeyFilePath}"/>
                                <Button Width="100" Content="ファイル">
                                    <b:Interaction.Triggers>
                                        <b:EventTrigger EventName="Click">
                                            <b:CallMethodAction MethodName="OpenPemFileDialog" TargetObject="{Binding}"/>
                                        </b:EventTrigger>
                                    </b:Interaction.Triggers>
                                </Button>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Margin="5" Width="100" VerticalAlignment="Center" Text="ユーザー名(MySQL)"/>
                                <TextBlock Margin="5" VerticalAlignment="Center" Text=":"/>
                                <TextBox Margin="5" Width="100" VerticalAlignment="Center" Text="{Binding MySQLSetting.MySQLUserID}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Margin="5" Width="100" VerticalAlignment="Center" Text="パスワード(MySQL)"/>
                                <TextBlock Margin="5" VerticalAlignment="Center" Text=":"/>
                                <TextBox Margin="5" Width="100" VerticalAlignment="Center" Text="{Binding MySQLSetting.MySQLPassword}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Margin="5" Width="100" VerticalAlignment="Center" Text="リモートパス"/>
                                <TextBlock Margin="5" VerticalAlignment="Center" Text=":"/>
                                <TextBox Margin="5" Width="350" VerticalAlignment="Center" Text="{Binding SSHConnection.RemoteDirectory}"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Margin="5" Width="100" VerticalAlignment="Center" Text="ローカルパス"/>
                                <TextBlock Margin="5" VerticalAlignment="Center" Text=":"/>
                                <TextBox Margin="5" Width="350" VerticalAlignment="Center" Text="{Binding SSHConnection.LocalDirectory}"/>
                                <Button Content="フォルダ選択">
                                    <b:Interaction.Triggers>
                                        <b:EventTrigger EventName="Click">
                                            <b:CallMethodAction MethodName="OpenFileBrowzeDialog" TargetObject="{Binding}"/>
                                        </b:EventTrigger>
                                    </b:Interaction.Triggers>
                                </Button>
                            </StackPanel>

                        </StackPanel>

                        <Grid Grid.Row="1">
                            <StackPanel Orientation="Horizontal">
                                <Button Margin="5" HorizontalAlignment="Left" Width="100" Content="設定を保存">
                                    <b:Interaction.Triggers>
                                        <b:EventTrigger EventName="Click">
                                            <b:CallMethodAction MethodName="SaveSetting" TargetObject="{Binding}"/>
                                        </b:EventTrigger>
                                    </b:Interaction.Triggers>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Grid>

                </GroupBox>
            </Grid>
            <GroupBox Margin="5" Grid.Column="1" Header="コマンドリスト">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <Button Margin="5" Width="100" HorizontalAlignment="Left" Content="圧縮処理の実行">
                            <b:Interaction.Triggers>
                                <b:EventTrigger EventName="Click">
                                    <b:CallMethodAction MethodName="ExecuteSsh" TargetObject="{Binding}"/>
                                </b:EventTrigger>
                            </b:Interaction.Triggers>
                        </Button>
                        <Button Margin="5" Width="100" HorizontalAlignment="Left" Content="ダウンロードの実行">
                            <b:Interaction.Triggers>
                                <b:EventTrigger EventName="Click">
                                    <b:CallMethodAction MethodName="ExecuteScp" TargetObject="{Binding}"/>
                                </b:EventTrigger>
                            </b:Interaction.Triggers>
                        </Button>

                    </StackPanel>
                    <GroupBox Header="結果" Grid.Row="1">
                        <TextBox VerticalScrollBarVisibility="Auto" IsReadOnly="True" Text="{Binding Message}"/>
                    </GroupBox>
                    <StackPanel Grid.Row="2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Margin="5" Text="ダウンロード(Themes)"/>
                            <TextBlock Margin="5" Text=":"/>
                            <TextBlock Margin="5" Text="{Binding DownloadProgress_themes}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Margin="5" Text="ダウンロード(Plugins)"/>
                            <TextBlock Margin="5" Text=":"/>
                            <TextBlock Margin="5" Text="{Binding DownloadProgress_plugin}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Margin="5" Text="ダウンロード(Uploads)"/>
                            <TextBlock Margin="5" Text=":"/>
                            <TextBlock Margin="5" Text="{Binding DownloadProgress_upload}"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Margin="5" Text="ダウンロード(Sql)"/>
                            <TextBlock Margin="5" Text=":"/>
                            <TextBlock Margin="5" Text="{Binding DownloadProgress_sql}"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </GroupBox>
        </Grid>
    </Grid>
</Window>
